<project name="My Project Name" default="build">
    <target name="deploy">
        <echo>${basedir} ${WORKSPACE}</echo>
        <echo message="....deploy.jboss ${basedir}"/> 
    </target>
</project>
