<project name="My Project Name" default="build">

    <property environment="env"/>

    <target name="deploy">
        <echo>${basedir} ${BuildDir} ${env.workspace} ${env.JOB_NAME}  ${env.WORKSPACE}</echo>
        <echo>${BuildDir}</echo>
        <echo>${env.BUILD_NUMBER}</echo>
        <echo>${env.JOB_NAME}</echo>
        <echo>${env.WORKSPACE}</echo>
    </target>
</project>
